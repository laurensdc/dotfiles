Symlink this to the /etc/nixos folder

```shell
sudo ln -s ~/.config/nixos/configuration.nix /etc/nixos/configuration.nix
sudo ln -s ~/.config/nixos/hardware-configuration.nix /etc/nixos/hardware-configuration.nix
```
